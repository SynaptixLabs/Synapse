# Epic O — Publish the brain as the GitHub Wiki (~40V) · GATED: founder go pending (public-facing)

> Up: [`../index.md`](../index.md) · Founder ask 2026-07-17: enhance
> `github.com/SynaptixLabs/Synapse/wiki`. The right version: **don't write a wiki —
> publish the self-brain AS the wiki** (GitHub wikis are git repos of markdown with
> `[[wikilinks]]` — exactly our vault format).

## Design (agreed in conversation)

- Exporter: vault → GitHub-wiki dialect (strip `.md` inside wikilinks, `Index.md` → `Home.md`,
  generated `_Sidebar.md` from top-connected notes, per-page banner: "Generated by SYNAPSE
  from this repo's markdown — edit the source, not here").
- `synapse wiki-publish` CLI (push to `<repo>.wiki.git`) + a GitHub Action re-publishing on
  every push to main. **Self-brain only** (the repo's own public markdown — zero-leak).
- Doc-location rule intact: the wiki is a DERIVED surface (same doctrine as graph.json);
  `project-management/` stays the single truth. Wiki edits locked to collaborators.
- Generalizes to a user feature: "publish YOUR brain as a wiki" (pairs with backlog #10 export).

## Tasks
- [ ] O1 exporter (dialect transform + Home + Sidebar + banner) with tests
- [ ] O2 `synapse wiki-publish` + Action; founder one-time: enable wiki / first page
- [ ] O3 README section; caveats (GitHub search, no asset images)
