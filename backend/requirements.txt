# Template baseline — just enough to run app/main.py and the example tests.
# Pin/extend per project (split out a requirements-dev.txt when it grows).
fastapi>=0.115
uvicorn[standard]>=0.30
pytest>=8
pytest-asyncio>=0.24
httpx>=0.27          # FastAPI TestClient transport (tests only)
anthropic>=0.40         # Distill provider — imported ONLY in modules/distill
openai>=1.50            # Render provider — imported ONLY in modules/render
pypdf>=4                # PDF text extraction for asset sidecars (small, pure-Python — the
                        # runtime degrades honestly without it, but tests exercise it)
